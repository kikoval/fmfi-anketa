{% macro displayResponse(responses) %}
    {% if responses is not empty %}
        <div class="results-comments results-comments-official">
        <h2>Oficiálne vyjadrenie k odpovediam:</h2>
        {% for response in responses %}
            <div class='comment' id="odpoved{{ response.id }}">
            <div class="comment-top">
            <b>{{ response.authorText }}</b>:
            </div>
            <p class="comment-comment">{{ response.comment }}</p>
            </div>
        {% endfor %}
        </div>
    {% endif %}
{% endmacro %}
